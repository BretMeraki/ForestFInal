services:
  - type: web
    name: forest-debug-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python flask_dashboard.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.9.0
    plan: free
